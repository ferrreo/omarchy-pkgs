# Generated by omapkg factory. Review before build.
pkgname='omapkg-bc'
pkgver='1.08.2'
pkgrel=1
pkgdesc='GNU bc is an arbitrary-precision calculator language bundled with GNU dc.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('readline')
makedepends=('bison' 'flex' 'texinfo')
source=('bc-1.08.2.tar.gz::https://ftp.gnu.org/gnu/bc/bc-1.08.2.tar.gz')
sha256sums=('ae470fec429775653e042015edc928d07c8c3b2fc59765172a330d3d87785f86')

build() {
  cd "$srcdir"
  cd "$srcdir/bc-1.08.2"
  ./configure --prefix=/usr --with-readline
  make
}

package() {
  cd "$srcdir/bc-1.08.2"
  make DESTDIR="$pkgdir" install
}
