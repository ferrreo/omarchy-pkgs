# Generated by OPR factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12'
pkgrel=3
pkgdesc='GNU Hello prints a friendly greeting message and serves as a canonical GNU autotools example program.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc')
makedepends=('gcc' 'make' 'gettext')
source=('hello-2.12.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz')
sha256sums=('cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab')

build() {
  cd "$srcdir"
  cd "$srcdir/hello-2.12"
  ./configure --prefix=/usr --build=${CHOST:-$(uname -m)-unknown-linux-gnu}
  make
}

package() {
  cd "$srcdir/hello-2.12"
  make DESTDIR=$pkgdir install
}
