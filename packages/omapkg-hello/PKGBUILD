# Generated by omapkg factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12'
pkgrel=6
pkgdesc='GNU Hello prints a friendly greeting and serves as a sample GNU package demonstrating autotools build conventions.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc')
makedepends=('autoconf' 'automake' 'gettext' 'help2man')
source=('hello-2.12.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz')
sha256sums=('cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab')

build() {
  cd "$srcdir"
  cd "$srcdir/hello-2.12"
  ./configure --prefix=/usr --build="${CHOST}"
  make
}

package() {
  cd "$srcdir/hello-2.12"
  make DESTDIR="${pkgdir}" install
}
