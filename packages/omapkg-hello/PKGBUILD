# Generated by OPR factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12'
pkgrel=1
pkgdesc='Upstream: GNU Hello 2.12 (stable, released 2022-01-31). Source archive verified by SHA-256 cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab against https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz.  License verification: requ'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc')
makedepends=('gcc' 'make' 'autoconf' 'automake' 'gettext' 'help2man' 'texinfo')
source=('hello-2.12.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz')
sha256sums=('cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab')

build() {
  cd "$srcdir"
  tar -xzf hello-2.12.tar.gz
  cd hello-2.12
  ./configure --prefix=/usr --disable-nls --build=$CHOST
  make -j"$(nproc)"
  make check
}

package() {
  cd "$srcdir"
  cd hello-2.12
  make DESTDIR="$pkgdir" install
  install -Dm644 COPYING "$pkgdir/usr/share/licenses/hello/COPYING"
  install -Dm644 README "$pkgdir/usr/share/doc/hello/README" 2>/dev/null || true
}
