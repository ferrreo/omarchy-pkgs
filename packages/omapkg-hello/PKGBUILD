# Generated by omapkg factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12.1'
pkgrel=1
pkgdesc='GNU Hello prints a friendly, customizable greeting and demonstrates the GNU autotools-based build system.'
arch=('x86_64' 'aarch64')
license=('GPL-3.0-or-later')
depends=('glibc' 'gettext')
makedepends=('gcc' 'make' 'gettext' 'texinfo')
source=('hello-2.12.1.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.1.tar.gz')
sha256sums=('8d99142afd92576f30b0cd7cb42a8dc6809998bc5d607d88761f512e26c7db20')

build() {
  cd "$srcdir"
  cd "$srcdir/hello-2.12.1"
  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/hello-2.12.1"
  make DESTDIR=$pkgdir install
}
