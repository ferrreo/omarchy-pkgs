# Generated by omapkg factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12.3'
pkgrel=4
pkgdesc='GNU Hello prints a friendly greeting and serves as a sample GNU package demonstrating autotools, gettext, gnulib, and Texinfo conventions.'
arch=('x86_64' 'aarch64')
license=('GPL-3.0-or-later')
depends=()
makedepends=('gettext')
source=('hello-2.12.3.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.3.tar.gz')
sha256sums=('0d5f60154382fee10b114a1c34e785d8b1f492073ae2d3a6f7b147687b366aa0')

build() {
  cd "$srcdir"
  cd "$srcdir/hello-2.12.3"
  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/hello-2.12.3"
  make DESTDIR="$pkgdir" install
}
