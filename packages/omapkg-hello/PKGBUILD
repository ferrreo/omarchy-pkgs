# Generated by OPR factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12'
pkgrel=4
pkgdesc='GNU Hello prints a friendly greeting and serves as a reference example of an autotools-based GNU package with gettext i18n support.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc' 'gettext')
makedepends=('gcc' 'make' 'gettext' 'autoconf' 'automake')
source=('hello-2.12.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz')
sha256sums=('cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab')

build() {
  cd "$srcdir"
  cd "$srcdir/hello-2.12"
  ./configure --prefix=/usr --build="${CHOST}"
  make
}

package() {
  cd "$srcdir/hello-2.12"
  make DESTDIR="$pkgdir" install
  install -Dm644 COPYING "$pkgdir/usr/share/licenses/$pkgname/COPYING"
}
