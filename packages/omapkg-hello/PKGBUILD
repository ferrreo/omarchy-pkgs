# Generated by OPR factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12'
pkgrel=2
pkgdesc='GNU Hello prints a friendly, customizable greeting and demonstrates GNU package build conventions.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc')
makedepends=('autoconf' 'automake' 'gettext' 'texinfo' 'help2man')
source=('hello-2.12.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz')
sha256sums=('cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab')

build() {
  cd "$srcdir"
  ./configure --prefix=/usr --disable-rpath --build=$(uname -m)-unknown-linux-gnu
  make
  make install DESTDIR=$pkgdir
}

package() {
  cd "$srcdir"
  find $pkgdir -name '*.la' -delete
}
