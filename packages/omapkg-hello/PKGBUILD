# Generated by OPR factory. Review before build.
pkgname='omapkg-hello'
pkgver='2.12'
pkgrel=1
pkgdesc='Inspected GNU hello 2.12 archive from ftp.gnu.org. SHA-256 cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab matches the published GNU checksum and the inspection report. Independent license verification: configure.ac carries'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc' 'gettext')
makedepends=('gcc' 'make' 'autoconf' 'automake' 'gettext' 'texinfo')
source=('hello-2.12.tar.gz::https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz')
sha256sums=('cf04af86dc085268c5f4470fbae49b18afbc221b78096aab842d934a76bad0ab')

build() {
  cd "$srcdir"
  cd hello-2.12
  ./configure --prefix=/usr
  make
  make check
}

package() {
  cd "$srcdir"
  cd hello-2.12
  make DESTDIR=${pkgdir} install
}
