# Generated by omapkg factory. Review before build.
pkgname='omapkg-nvidia-tools'
pkgver='610.57.04'
pkgrel=3
pkgdesc='Proprietary NVIDIA System Management Interface (nvidia-smi) and NVML user-space library, packaged for recipe-only distribution.'
arch=('x86_64')
license=('LicenseRef-NVIDIA-EULA')
depends=('glibc' 'libgcc' 'zlib')
makedepends=()
source=('NVIDIA-Linux-x86_64-610.57.04.run::https://download.nvidia.com/XFree86/Linux-x86_64/610.57.04/NVIDIA-Linux-x86_64-610.57.04.run')
sha256sums=('b2e935c66b83bb00c0c857bc8e0ee0fd52de9286b40c9cc1eec29a7ce7eb116d')

build() {
  cd "$srcdir"
  set -eu
  source="$srcdir/NVIDIA-Linux-x86_64-610.57.04.run"
  destination="$srcdir/vendor-root"
  printf '%s  %s\n' 'b2e935c66b83bb00c0c857bc8e0ee0fd52de9286b40c9cc1eec29a7ce7eb116d' "$source" | sha256sum -c -
  rm -rf "$destination"
  sh "$source" --extract-only --target "$destination"
  echo 'Skipping build: the platform renderer extracts the .run bundle into $srcdir/vendor-root. No in-tree compilation; binaries are staged as-is from vendor-root.'
}

package() {
  cd "$srcdir"
  install -dm755 "$pkgdir/usr/bin"
  install -dm755 "$pkgdir/usr/lib"
  install -m755 vendor-root/nvidia-smi "$pkgdir/usr/bin/nvidia-smi"
  install -m755 vendor-root/libnvidia-ml.so.610.57.04 "$pkgdir/usr/lib/libnvidia-ml.so.610.57.04"
  ln -s libnvidia-ml.so.610.57.04 "$pkgdir/usr/lib/libnvidia-ml.so.1"
  install -dm755 "$pkgdir/usr/share/licenses/$pkgname"
  install -m644 vendor-root/LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
