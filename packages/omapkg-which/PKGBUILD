# Generated by omapkg factory. Review before build.
pkgname='omapkg-which'
pkgver='2.25'
pkgrel=6
pkgdesc='GNU utility that prints the full path of executables found in PATH.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('glibc')
makedepends=()
source=('which-2.25.tar.gz::https://ftp.gnu.org/gnu/which/which-2.25.tar.gz')
sha256sums=('1cb83e4f702e60b8211ab5ec4c2afbab1b1dec80209456a7d2faf7584ed225ea')

build() {
  cd "$srcdir"
  cd "$srcdir/which-2.25"
  ./configure --prefix=/usr
  make
}

package() {
  cd "$srcdir/which-2.25"
  make DESTDIR="$pkgdir" install
}
