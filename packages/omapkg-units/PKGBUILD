# Generated by omapkg factory. Review before build.
pkgname='omapkg-units'
pkgver='2.27'
pkgrel=3
pkgdesc='GNU units converts quantities between measurement systems and performs nonlinear conversions with dimensional analysis.'
arch=('x86_64')
license=('GPL-3.0-or-later')
depends=('readline')
makedepends=('bison' 'python')
source=('units-2.27.tar.gz::https://ftp.gnu.org/gnu/units/units-2.27.tar.gz')
sha256sums=('e1bbdb09672e7c08eee986749e7a1629eb84a6bdf41f5a2a79d6804444abbe10')

build() {
  cd "$srcdir"
  cd "$srcdir/units-2.27"
  cd "$srcdir/units-2.27"
  ./configure --prefix=/usr --sharedstatedir=/var/lib
  make
}

package() {
  cd "$srcdir/units-2.27"
  cd "$srcdir/units-2.27"
  make install DESTDIR="$pkgdir"
  install -Dm644 COPYING "$pkgdir/usr/share/licenses/$pkgname/COPYING"
}
